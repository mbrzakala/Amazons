# GLOSSARY — fixed names, no synonyms

The only accepted names for things across all sessions. Before using a new name for a
domain entity, component, token, or service anywhere, add it here. Never rewrite a
term's meaning once other files reference it — supersede it in the owning stage instead.

No empty or placeholder rows. If the table is short, that is correct.

| Term | Means | Never call it |
|---|---|---|
